import RoutePage from "@/components/RoutePage";

export default function StudioPage() {
  return (
    <RoutePage
      title="Studio"
      description="Compose interior concepts, upload room references, and generate polished design proposals."
      ctaHref="/dashboard"
      ctaLabel="Open Dashboard"
    />
  );
}
